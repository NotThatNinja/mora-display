<section class="contact" id="contact">
  <div class="contact-background">
    <div class="bg-logo-placeholder"></div>
  </div>
  <div class="container">
    <div class="section-with-vertical-header">
      <div class="vertical-header">
        <h2 class="vertical-title">CONTACT</h2>
        <div class="vertical-line"></div>
      </div>
      <div class="contact-content">
        <div class="contact-info-mobile">
          <div class="company-info">
            <h3>MORADISPLAY</h3>
            <div class="info-item">
              <p>Via Cefalonia 7, 20124 Milano, Italy</p>
            </div>
            <div class="info-item">
              <p>Tel: +39 02 6707 0334</p>
            </div>
            <div class="info-item">
              <p>Email: info@moradisplay.com</p>
            </div>
            <div class="info-item">
              <p>www.moradisplay.com</p>
            </div>
          </div>
        </div>
        <div class="contact-form-section">
          <form class="contact-form" action="#" method="post">
            <div class="form-group">
              <input type="text" name="name" placeholder="NAME" required />
            </div>
            <div class="form-group">
              <input type="email" name="email" placeholder="EMAIL" required />
            </div>
            <div class="form-group">
              <textarea name="message" placeholder="MESSAGE" rows="8" required></textarea>
            </div>
            <button type="submit" class="submit-btn">SEND</button>
          </form>
        </div>
        <div class="contact-info-desktop">
          <div class="company-info">
            <h3>MORADISPLAY</h3>
            <div class="info-item">
              <p>Via Cefalonia 7, 20124 Milano, Italy</p>
            </div>
            <div class="info-item">
              <p>Tel: +39 02 6707 0334</p>
            </div>
            <div class="info-item">
              <p>Email: info@moradisplay.com</p>
            </div>
            <div class="info-item">
              <p>www.moradisplay.com</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    padding: 10rem 0;
    background: var(--bg-darker);
    position: relative;
  }

  .contact-background {
    position: relative;
  }

  .bg-logo-placeholder {
    position: absolute;
    right: 5%;
    top: 50%;
    transform: translateY(-50%);
    width: 250px;
    height: 180px;
    background: var(--logo-gray);
    opacity: 0.05;
    z-index: 1;
    pointer-events: none;
    border-radius: 10px;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
    position: relative;
    z-index: 2;
    padding-top: 2rem;
  }

  .contact-info-mobile {
    display: none;
  }

  .contact-form-section {
    display: flex;
    justify-content: center;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    width: 100%;
    max-width: 400px;
  }

  .form-group {
    position: relative;
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 1rem;
    background: var(--bg-darker);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 5px;
    color: var(--text-white);
    font-size: 1rem;
    font-family: inherit;
    transition: border-color 0.3s ease;
    resize: vertical;
  }

  .form-group textarea {
    min-height: 120px;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-orange);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: var(--text-white);
    font-size: 0.9rem;
    letter-spacing: 0.5px;
  }

  .submit-btn {
    padding: 1rem 2rem;
    background: var(--primary-orange);
    color: var(--text-white);
    border: none;
    border-radius: 5px;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 1px;
    cursor: pointer;
    transition: background 0.3s ease;
  }

  .submit-btn:hover {
    background: #e55a2b;
  }

  .contact-info-desktop {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .company-info {
    text-align: center;
  }

  .company-info h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary-orange);
    margin-bottom: 2rem;
    letter-spacing: 1px;
  }

  .info-item {
    margin-bottom: 1rem;
  }

  .info-item p {
    color: var(--primary-orange);
    line-height: 1.6;
    font-size: 1.1rem;
  }

  @media (max-width: 768px) {
    .contact {
      padding: 6rem 0;
    }
    
    .contact-content {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding-top: 0;
    }

    .contact-info-mobile {
      display: block;
      order: -1;
    }

    .contact-info-desktop {
      display: none;
    }

    .company-info {
      text-align: left;
    }
    
    .contact-form {
      max-width: 100%;
    }
    
    .bg-logo-placeholder {
      display: none;
    }
  }
</style>